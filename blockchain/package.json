{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "DID, Identity Registry, and Smart Contracts for Blockchain Platform",
  "scripts": {
    "compile": "hardhat compile",
    "test": "HARDHAT_TELEMETRY_OPTOUT=true hardhat test",
    "deploy:local": "hardhat run scripts/deploy-identity.ts --network localhost",
    "deploy:hardhat": "hardhat run scripts/deploy-identity.ts",
    "export-abi": "hardhat run scripts/export-abi.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.14.10",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.6",
    "typescript": "^5.5.3"
  }
}
